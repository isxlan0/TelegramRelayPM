# ============================
# TelegramRelayPM 配置示例
#
# 使用方法：复制为 .env 并修改对应值。
# 注意：若值中包含 # / 空格 / 链接，务必使用双引号包裹，避免被当作注释截断。
# ============================

# --- 必填 ---
BOT_TOKEN="123456:replace_with_your_bot_token"

# 管理员“用户ID”(不是群ID)。支持多个管理员，用 | 分隔。
ADMIN_CHAT_ID="123456789|987654321"

# --- 转发模式（全局二选一）---
# private     : 用户私聊 bot -> 转发到管理员私聊；管理员通过回复或 /session 回传
# group_topic : 用户私聊 bot -> 转发到管理员论坛超级群的用户话题；管理员在话题内直接发即可回传
RELAY_MODE="private"

# --- group_topic 模式专用（RELAY_MODE=group_topic 时必填）---
# 管理员论坛超级群 ID（必须开启群话题），ID获取方法（随便点个话题进去https://t.me/c/xxx/1，这里的xxxx就是群ID）
# 推荐直接填写数字部分：1234567890（程序会自动转换为 -1001234567890）
# 也兼容填写完整 -100... 或 https://t.me/c/1234567890/1
# 可在群内发送 /chatid 获取 Chat ID。
ADMIN_GROUP_CHAT_ID="1234567890"

# 可选：指定管理员群中 General/运维话题 Thread ID。
# 目前代码仅用于 /chatid 显示（预留扩展用）。
# 可在对应话题内发送 /chatid 获取 Thread ID。
ADMIN_GROUP_GENERAL_THREAD_ID="1"

# --- 数据库 ---
DB_PATH="relay_bot.db"

# --- 广播节流 ---
# 广播每个用户之间的发送间隔（秒）。建议 1 秒/用户，降低触发 Telegram 频控的风险。
BROADCAST_DELAY_SECONDS="1.0"

# --- /start 公告（仅使用 START_MESSAGE；无 DB 动态公告）---
# 多行请用 \n
START_MESSAGE="公告：这里是双向消息中继机器人。\n\n喵呜~"

# --- Bot 元数据自动同步（启动时同步到 Telegram）---
BOT_NAME="双向中继机器人"
BOT_VERSION="v1.1.2"

# 多行请用 \n
BOT_DESCRIPTION="这是一个双向消息中继机器人。\n通过本机器人转发消息。"
BOT_SHORT_DESCRIPTION="双向消息中继"

# --- 命令菜单（启动时同步到 Telegram）---
# 格式：command:description;command:description
# 注意：command 不要写 / 前缀

BOT_USER_COMMANDS="start:查看公告与说明;id:查看你的用户ID;version:查看机器人版本"

# 建议包含 chatid，方便在群/话题内获取 chat_id/thread_id
BOT_ADMIN_COMMANDS="start:查看公告与说明;id:查看你的用户ID;chatid:查看当前群组和话题ID;version:查看机器人版本;recent:管理员查看最近活跃用户;session:管理员切换当前会话;ban:管理员封禁用户;unban:管理员解封用户;sender:管理员获取发送者ID;broadcast:管理员广播消息;deletepair:管理员删除消息"
